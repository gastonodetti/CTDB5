no usar los dto en los services para guardar info - solo se usan para traer info



Crear turno service:

 Long pacienteId = turno.getPaciente().getId();
        Long odontologoId = turno.getOdontologo().getId();
        
        Paciente paciente = mapper.convertValue(pacienteService.buscarPaciente(pacienteId), Paciente.class);
        Odontologo odontologo = mapper.convertValue(odontologoService.buscarOdontologo(odontologoId), Odontologo.class);
        
        turno.setPaciente(paciente);
        turno.setOdontologo(odontologo);
